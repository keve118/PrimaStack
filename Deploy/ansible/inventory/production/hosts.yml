all:
  children:
    zabbix_servers:
      hosts:
        zabbix-one:
          ansible_host: 10.0.1.11
        zabbix-two:
          ansible_host: 10.0.1.21

    zabbix_database:
      hosts:
        zabbix-db:
          ansible_host: 10.0.1.23
